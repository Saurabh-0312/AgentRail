// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

import {Test} from "forge-std/Test.sol";

/// Keeps `forge test` green until the AgentRailRegistry tests land. Delete when they do.
contract PlaceholderTest is Test {
    function test_placeholderUntilRegistryTestsLand() public pure {
        assertTrue(true);
    }
}
