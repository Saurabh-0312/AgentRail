[profile.default]
src = "src"
out = "out"
libs = ["lib"]
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
ignored_warnings_from = ["lib/"]

[rpc_endpoints]
sepolia = "${SEPOLIA_RPC_URL}"
hedera_testnet = "${HEDERA_JSON_RPC_URL}"
