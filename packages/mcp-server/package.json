{
  "name": "@agentrail/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AgentRail MCP server: any agent can read its own mandate, check whether a payment would be permitted, list the services it can discover, and read its history. Read-only; it never signs.",
  "scripts": {
    "start": "node --env-file=../../.env src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentrail/query": "0.1.0",
    "@agentrail/sdk": "0.1.0",
    "@modelcontextprotocol/sdk": "^1.12",
    "@solana/spl-token": "^0.4.9",
    "@solana/web3.js": "^1.98.0",
    "viem": "^2.48.11",
    "zod": "^3.25"
  },
  "devDependencies": {
    "vitest": "^4"
  },
  "engines": {
    "node": ">=22.12"
  }
}
