# dependencies / build output
node_modules/
target/
dist/
build/
.next/
out/

# anchor / solana
.anchor/
test-ledger/
**/*.rs.bk
.yarn/

# foundry
contracts/cache/
contracts/out/
contracts/broadcast/

# env / secrets — never commit
.env
.env.*
!.env.example
*.pem
*-keypair.json

# os / editor
.DS_Store
*.log

# never commit pre-existing design material
*.pdf

# Phase 0 verification spikes stay local; never part of the submission
spikes/

# substreams auth writes the Graph Market JWT here
.substreams.env
